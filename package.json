{
	"name": "vue-popup-plus-root",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"engines": {
		"node": "^20.19.0 || >=22.12.0"
	},
	"scripts": {
		"dev": "yarn workspace playground dev",
		"docs:dev": "yarn workspace doc dev",
		"docs:build": "yarn workspace doc build",
		"docs:preview": "yarn workspace doc preview",
		"lib:build": "yarn workspace vue-popup-plus build",
		"lib:types": "yarn workspace vue-popup-plus build:types",
		"plugin:build": "yarn workspace vue-popup-plus-plugin-preset build"
	}
}

