{
	"name": "vue-popup-plus-root",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"engines": {
		"node": "^20.19.0 || >=22.12.0"
	},
	"scripts": {
		"dev": "yarn workspace playground dev",
		"doc:dev": "yarn workspace doc dev",
		"doc:build": "yarn workspace doc build",
		"lib:build": "yarn workspace vue-popup-plus build",
		"plugin:build": "yarn workspace vue-popup-plus-plugin-preset build"
	}
}

