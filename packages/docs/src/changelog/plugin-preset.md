---
pageClass: changelog
outline: 2
---

# 插件预设 更新日志

## 1.4.1

### 特性

- 所有功能统一支持皮肤配置项，包括 `经典皮肤 classic` 和 `现代皮肤 modern`，默认使用 `经典皮肤 classic`，即原有样式。

### 适配

- 适配核心 `1.4.1` 版本。

## 1.4.0

### 特性

- 独立导出每个子功能插件，允许开发者单个插件独立使用。
- 所有功能使用统一主题配置。
- `Album 媒体相册` 默认使用高斯模糊遮罩层。

### 优化

- 提供更完整的类型推断和代码提示。

### 破坏性改动

::: danger

- 移除默认导出，改为具名导出 `presetPlugin`。
- `Loading 加载遮罩` 参数优化，和其他功能保持统一。
- `Prompt 提示输入` 参数优化，和其他功能保持统一。
  :::

### 适配

- 适配核心 `1.4.0` 版本。

## 1.3.4

### 修复

- 修复 `Dialog 对话` 最大宽度和最大高度无法正确生效的问题。

### 优化

- 优化 `Toast 消息` 当文本超出最大高度时，允许滚动查看完整内容。

### 适配

- 适配核心 `1.3.4` 版本。

## 1.3.3

### 特性

- `Alert 提示` 、 `Confirm 确认` 、 `Prompt 提示输入` 、 `Dialog 对话` 统一支持标题栏关闭按钮隐藏，禁用高斯模糊遮罩配置项。
- 所有功能新增是否启用遮罩层高斯模糊配置项。

### 修复

- 修复 `Prompt 提示输入` 的输入控件在暗色主题下无法正确适配的问题。
- 修复 `Alert 提示` 、 `Confirm 确认` 、 `Prompt 提示输入` 、 `Dialog 对话` 标题栏样式未生效的问题。

### 优化

- 优化展示效果，提供更加细腻的渲染细节。

### 破坏性改动

::: danger

- 将 `Dialog 对话` 的渲染参数 `props` 改为 `componentProps` ，和 `render()` 函数保持统一。
- `Prompt 提示输入` 返回值类型从原有的 `string | null` 改为 `string | void`

:::

### 适配

- 适配核心 `1.3.3` 版本。

## 1.3.2

### 优化

- 优化亮色主题的显示效果。

### 适配

- 适配核心 `1.3.2` 版本。

## 1.3.1

### 优化

- 优化 `Toast 消息` 的动画效果，使用 `缩放放大` 以提高用户关注度。

### 适配

- 适配核心 `1.3.1` 版本。

## 1.3.0

### 特性

- 使用 `css 颜色变量` 支持自定义主题颜色。
- `Toast 消息` 支持自定义主题。

### 修复

- 修复加载遮罩 `Loading` 显示层级错误的问题。

### 适配

- 适配核心 `1.3.0` 版本。

## 1.2.1

### 适配

- 适配核心 `1.2.1` 版本。

## 1.2.0

### 适配

- 适配 `vue 3.5.22` 版本，提供更好的兼容性和性能。
- 适配核心 `1.2.0` 版本。

## 1.1.5

### 适配

- 适配核心 `1.1.5` 版本。

## 1.1.4

### 适配

- 适配核心 `1.1.4` 版本。

## 1.1.3

### 适配

- 适配核心 `1.1.3` 版本。

## 1.1.2

### 适配

- 适配核心 `1.1.2` 版本。

## 1.1.1

### 适配

- 适配核心 `1.1.1` 版本。

## 1.1.0

### 适配

- 适配核心 `1.1.0` 版本。

## 1.0.0

### 特性

- 从 [vue-popup](https://github.com/styzy/vue-popup) 中将插件模块独立出来，作为一个独立的插件模块，方便开发者使用。
