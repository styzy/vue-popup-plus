# 安装

只需要在你的 `Vue` 项目中运行以下命令即可安装：

::: code-group

```bash [npm]
npm install vue-popup-plus-plugin-preset
```

```bash [yarn]
yarn add vue-popup-plus-plugin-preset
```

```bash [pnpm]
pnpm add vue-popup-plus-plugin-preset
```
