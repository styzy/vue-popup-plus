export interface AnimationTypes {
	FADE: string
	SCALE: string
	FLY: string
}
/**
 * 动画类型
 * - `ANIMATION_TYPES.FADE` 淡入淡出
 * - `ANIMATION_TYPES.SCALE` 缩放
 * - `ANIMATION_TYPES.FLY` 飞入
 */
export const ANIMATION_TYPES: AnimationTypes = {
	FADE: 'fade',
	SCALE: 'scale',
	FLY: 'fly',
}
